<UserControl x:Class="PharmacyStockManager.Views.ProductsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="10">
        <DataGrid x:Name="dgProducts" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding ProductID}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category.CategoryName}" Width="*"/>
                <DataGridTextColumn Header="Supplier" Binding="{Binding Supplier.SupplierName}" Width="*"/>
                <DataGridTextColumn Header="Batch" Binding="{Binding BatchNumber}" Width="*"/>
                <DataGridTextColumn Header="Expiry" Binding="{Binding ExpiryDate}" Width="*"/>
                <DataGridTextColumn Header="Purchase Price" Binding="{Binding PurchasePrice}" Width="*"/>
                <DataGridTextColumn Header="Selling Price" Binding="{Binding SellingPrice}" Width="*"/>
                <DataGridTextColumn Header="Stock" Binding="{Binding QuantityInStock}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>

<Window x:Class="PharmacyStockManager.Views.PopupWindows.CustomerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Customer" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize" Height="360" Width="400" Background="White">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Customer" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="1" Margin="0,5,0,0">
            <TextBlock Text="Customer Name:" FontWeight="SemiBold"/>
            <TextBox x:Name="txtName" Height="30" Margin="0,5,0,10" Style="{StaticResource ValidationTextbox}" Text="{Binding CustomerName,UpdateSourceTrigger=LostFocus,ValidatesOnDataErrors=True}"/>
        </StackPanel>
        <StackPanel Grid.Row="2">
            <TextBlock Text="Phone Number:" FontWeight="SemiBold"/>
            <TextBox x:Name="txtPhone" Height="30" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5,0,10" Style="{StaticResource ValidationTextbox}" Text="{Binding PhoneNumber,UpdateSourceTrigger=LostFocus,ValidatesOnDataErrors=True}"/>
        </StackPanel>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Height="30" Margin="0,10,10,0" Background="#4CAF50" Style="{StaticResource RoundedActionButton}" Command="{Binding SaveCommand}"  />
            <Button Content="Cancel" Width="80" Height="30" Margin="0,10,0,0" Background="LightGray" Style="{StaticResource RoundedActionButton}" Foreground="Black" Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>
</Window>

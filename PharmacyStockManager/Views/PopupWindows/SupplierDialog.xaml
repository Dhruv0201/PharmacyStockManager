<ext:ChildWindow x:Class="PharmacyStockManager.Views.PopupWindows.SupplierDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ext="http://schemas.xceed.com/wpf/xaml/toolkit"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PharmacyStockManager.Views.PopupWindows"
        mc:Ignorable="d"
        CaptionForeground="Black"
        CaptionFontSize="14"
         Background="White">

    <ext:ChildWindow.Caption>
        Add / Edit Supplier
    </ext:ChildWindow.Caption>
    <Grid Margin="15" Width="320">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Supplier" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="1" Margin="0,5,0,0">
            <TextBlock Text="Supplier Name:" FontWeight="SemiBold"/>
            <TextBox x:Name="txtSupplierName" Height="30" Margin="0,5,0,10"
                 Style="{StaticResource ValidationTextbox}"
                 Text="{Binding SupplierName, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True}"/>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="Mobile Number:" FontWeight="SemiBold"/>
            <TextBox x:Name="txtMobile" Height="30" Margin="0,5,0,10"
                 Style="{StaticResource ValidationTextbox}"
                 Text="{Binding MobileNumber, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True}"/>
        </StackPanel>

        <StackPanel Grid.Row="3">
            <TextBlock Text="Email:" FontWeight="SemiBold"/>
            <TextBox x:Name="txtEmail" Height="30" Margin="0,5,0,10"
                 Style="{StaticResource ValidationTextbox}"
                 Text="{Binding Email, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True}"/>
        </StackPanel>

        <StackPanel Grid.Row="4">
            <TextBlock Text="Address:" FontWeight="SemiBold"/>
            <TextBox x:Name="txtAddress" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,5,0,10"
                 Style="{StaticResource ValidationTextbox}"
                 Text="{Binding Address, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True}"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" Width="80" Height="30" Margin="0,10,10,0"
                Background="#4CAF50" Style="{StaticResource RoundedActionButton}"
                Command="{Binding SaveCommand}"/>
            <Button Content="Cancel" Width="80" Height="30" Margin="0,10,0,0"
                Background="LightGray" Foreground="Black" 
                Style="{StaticResource RoundedActionButton}"
                Command="{Binding CancelCommand}"/>
        </StackPanel>
    </Grid>

</ext:ChildWindow>

<ext:ChildWindow x:Class="PharmacyStockManager.Views.PopupWindows.PurchaseReturnDetailDialog"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ext="http://schemas.xceed.com/wpf/xaml/toolkit"
                 xmlns:mat="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf"
                 IsModal="True"
                 CaptionForeground="Black"
                 CaptionFontSize="14"
                 WindowStartupLocation="Center"
                 Background="White">

    <ext:ChildWindow.Caption>
        Add / Edit Purchase Return Detail
    </ext:ChildWindow.Caption>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Purchase Return Detail"
                       FontSize="20"
                       FontWeight="Bold"
                       Margin="0,0,0,15"/>

            <StackPanel Grid.Row="1" VerticalAlignment="Top">

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Product:" FontWeight="SemiBold"/>
                    <ComboBox x:Name="cbProduct"
                              ItemsSource="{Binding Products}"
                              SelectedItem="{Binding SelectedProduct, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True}"
                              DisplayMemberPath="ProductName"
                              SelectedValuePath="ProductId"
                              Style="{StaticResource ValidationCombobox}"
                              Height="30"/>
                </StackPanel>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Batch Number:" FontWeight="SemiBold"/>
                    <TextBox x:Name="txtBatchNumber"
                             Height="30"
                             Text="{Binding BatchNumber, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True}"
                             Style="{StaticResource ValidationTextbox}"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <StackPanel Width="200" Margin="0,0,20,0">
                        <TextBlock Text="Quantity Returned:" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtQuantity"
                                 Height="30"
                                 Text="{Binding QuantityReturned, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True}"
                                 PreviewTextInput="txtQuantity_PreviewTextInput"
                                 Style="{StaticResource ValidationTextbox}"/>
                    </StackPanel>

                    <StackPanel Width="200">
                        <TextBlock Text="Unit Price:" FontWeight="SemiBold"/>
                        <TextBox x:Name="txtUnitPrice"
                                 Height="30"
                                 Text="{Binding UnitPrice, UpdateSourceTrigger=LostFocus, ValidatesOnDataErrors=True}"
                                 PreviewTextInput="txtQuantity_PreviewTextInput"
                                 Style="{StaticResource ValidationTextbox}"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Margin="0,0,0,10">
                    <TextBlock Text="Total Price:" FontWeight="SemiBold"/>
                    <TextBox x:Name="txtTotalPrice"
                             Height="30"
                             Text="{Binding TotalPrice, UpdateSourceTrigger=LostFocus}"
                             IsReadOnly="True"
                             Background="#F5F5F5"
                             BorderBrush="#CCC"/>
                </StackPanel>

            </StackPanel>

            <StackPanel Grid.Row="2"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Margin="0,15,0,0">

                <Button Content="Save"
                        Command="{Binding SaveCommand}"
                        Width="90" Height="35"
                        Margin="0,0,10,0"
                        Background="#4CAF50"
                        Foreground="White"
                        Style="{StaticResource RoundedActionButton}"/>

                <Button Content="Cancel"
                        Width="90" Height="35"
                        Command="{Binding CancelCommand}"
                        Background="LightGray"
                        Foreground="Black"
                        Style="{StaticResource RoundedActionButton}"/>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</ext:ChildWindow>

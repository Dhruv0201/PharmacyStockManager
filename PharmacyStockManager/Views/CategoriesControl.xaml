<UserControl x:Class="PharmacyStockManager.Views.CategoriesControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:PharmacyStockManager.ViewModel"
             Background="White">


    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header: Filters + CRUD -->
        <Border Grid.Row="0" Background="#F7F7F7" Padding="10" CornerRadius="5" Margin="0,0,0,10">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Content="Add" Width="80" Height="30" Command="{Binding AddCommand}" 
                            Style="{StaticResource RoundedActionButton}" Background="Orange" Margin="5,0"/>
                    <Button Content="Refresh" Width="80" Height="30" Command="{Binding RefreshCommand}"
                            Style="{StaticResource RoundedActionButton}" Background="LightGray" Foreground="Black" Margin="5,0"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <TextBlock Text="Search:" VerticalAlignment="Center" FontWeight="SemiBold" Margin="0,0,5,0"/>
                    <TextBox Width="200" Height="28" VerticalContentAlignment="Center"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,5,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- DataGrid -->
        <DataGrid x:Name="dgCategories" Grid.Row="1"
                  AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True"
                  ItemsSource="{Binding Categories}" SelectionMode="Single" AlternatingRowBackground="#F9F9F9"
                  RowHeaderWidth="0" BorderThickness="1" BorderBrush="#DDD">
            <DataGrid.Columns>
                <DataGridTextColumn Header="SrNo." Binding="{Binding CategoryID}" Width="Auto"/>
                <DataGridTextColumn Header="Name" Binding="{Binding CategoryName}" Width="2*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="3*"/>
                <DataGridTemplateColumn Header="Actions" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Edit" Command="{Binding DataContext.EditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" Margin="5,0"/>
                                <Button Content="Delete" Command="{Binding DataContext.DeleteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" Margin="5,0"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
